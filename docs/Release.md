# Release

A release must have a clean version identifier, updated release notes, passing smoke tests, and passing integration tests. Full packages contain the complete repository snapshot; update packages contain only files changed from the previous release.
